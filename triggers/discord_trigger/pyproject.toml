[project]
name = "discord-trigger"
version = "0.1.0"
description = "Discord Webhook Events trigger for Dify"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.10.2",
    "pynacl>=1.6.2",
    "werkzeug>=3.1.8",
]

[dependency-groups]
dev = []
