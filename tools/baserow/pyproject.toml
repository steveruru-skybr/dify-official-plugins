[project]
name = "baserow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "baserowapi>=0.2.0b1",
    "dify_plugin>=0.10.2",
    "pydantic>=2.13.5",
]

[dependency-groups]
dev = []
