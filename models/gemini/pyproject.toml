[project]
name = "gemini-g9cIE8"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

# Managed with uv; refresh the lockfile with `uv lock`.
dependencies = [
    # 0.10.0 introduces get_current_session(), required to resolve the
    # current Dify app_id for Gemini request labels.
    "dify-plugin>=0.10.2",
    "google-genai>=2.22.0",
    "numpy>=2.5.3",
    "requests>=2.34.2",
]

# uv run black . && uv run ruff check --fix && uv run ruff format
[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
    "python-dotenv>=1.2.3",
    "Pillow>=12.3.0",
    "black>=26.5.1",
    "pyyaml>=6.0.3",
]

[tool.pytest.ini_options]
# Pytest configuration for Gemini plugin tests
addopts = "--tb=short"
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

# Test discovery paths
testpaths = ["models/tests/"]
pythonpath = ["."]

# Markers
markers = [
    "integration: marks integration tests",
    "live: sends billable requests to the real Gemini API",
    "unit: marks tests as unit tests (no API key required)"
]

# Logging configuration
log_cli = true
log_cli_level = "INFO"
log_cli_format = "%(asctime)s [%(levelname)8s] %(name)s: %(message)s"
log_cli_date_format = "%Y-%m-%d %H:%M:%S"

# Warnings
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
    "ignore:Monkey-patching ssl after ssl has already been imported:gevent.monkey.MonkeyPatchWarning"
]

[tool.black]
# Equivalent to: uv run black -C -l 100
line-length = 100
skip-string-normalization = true
include = '(^|/)llm\.py$'

[tool.ruff.lint]
# Ruff 0.16 expanded its defaults; keep the existing lint contract explicit.
select = ["E4", "E7", "E9", "F"]
